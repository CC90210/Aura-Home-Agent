# Environment & secrets
.env
.env.*
!.env.example

# Security audit — local rotation timestamp (never commit, tracked per-machine)
.ha_token_last_rotated

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# Node (if any tooling needs it)
node_modules/
.next/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Home Assistant local testing
.storage/
home-assistant.log

# Claude Code
.claude/

# Recordings / audio (clap detection testing)
*.wav
*.mp3
*.flac
recordings/
repo

# Runtime data (SQLite databases, state files, learned patterns)
data/*
memory/
# EXCEPTION: Cross-agent pulse files are tracked (4-agent protocol)
!data/pulse/
data/pulse/*
!data/pulse/*.json
!data/pulse/*.md
*.db
*.db-wal
*.db-shm

# Voice agent generated models (trained on-device)
voice-agent/models/*.onnx
